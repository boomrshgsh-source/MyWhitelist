--================= SWIFT HUB WHITELIST =================--

local GIST_RAW   = "https://gist.githubusercontent.com/boomrshgsh-source/06a3e054df764e01ec58feb6889b87f9/raw/897efe302d11ef1df6b63386851dbe2ff63033b2/whitelist.json"
local GIST_API   = "https://api.github.com/gists/06a3e054df764e01ec58feb6889b87f9"
local TOKEN      = "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"
local MAIN_SCRIPT = "https://raw.githubusercontent.com/Sawarddekub/Quality-/refs/heads/main/luraph"

local Key = _G.Key

local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local RbxAnalyticsService = game:GetService("RbxAnalyticsService")
local LocalPlayer = Players.LocalPlayer
local HWID = RbxAnalyticsService:GetClientId()

local function Kick(msg)
    LocalPlayer:Kick(msg or "[Swift HUB]\nInvalid Key")
end

if not Key then
    Kick("[Swift HUB]\nPlease insert key")
end

-- โหลด DB
local success, DB = pcall(function()
    return HttpService:JSONDecode(game:HttpGet(GIST_RAW))
end)
if not success or type(DB) ~= "table" then
    Kick("[Swift HUB]\nCannot load database")
end

local data = DB[Key]
if not data then
    Kick("[Swift HUB]\nInvalid Key")
end

-- เช็ควันหมดอายุ
if data.expired and tonumber(data.expired) ~= 0 then
    if os.time() > tonumber(data.expired) then
        Kick("[Swift HUB]\nKey expired")
    end
end

-- ============ AUTO HWID BIND ============
if not data.hwid or data.hwid == "" then
    DB[Key].hwid = HWID

    local body = HttpService:JSONEncode({
        files = {
            ["whitelist.json"] = {
                content = HttpService:JSONEncode(DB)
            }
        }
    })

    local ok = pcall(function()
        game:HttpRequest({
            Url = GIST_API,
            Method = "PATCH",
            Headers = {
                ["Authorization"] = "token " .. TOKEN,
                ["Content-Type"] = "application/json"
            },
            Body = body
        })
    end)

    if not ok then
        Kick("[Swift HUB]\nHWID bind failed")
    end
else
    if tostring(data.hwid) ~= tostring(HWID) then
        Kick("[Swift HUB]\ninvalid Hwid")
    end
end

-- ผ่านไวลิส
loadstring(game:HttpGet(https://raw.githubusercontent.com/Sawarddekub/Quality-/refs/heads/main/luraph))()
